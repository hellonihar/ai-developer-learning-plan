CLI Calculator → Students run python calculator.py and practice input/output logic.

File Parser → Parse CSVs to show data handling basics.

Flask App + Docker → Run docker build -t flask-demo . then docker run -p 5000:5000 flask-demo to demo containerization.